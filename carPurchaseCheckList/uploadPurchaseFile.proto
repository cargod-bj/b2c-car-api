syntax = "proto3";

package uploadPurchaseFile;

import "github.com/cargod-bj/b2c-proto-common/common/common.proto";

option go_package = "github.com/cargod-bj/b2c-car-api/uploadPurchaseFile";


service UploadPurchaseFile {
  // 保存文件上传地址到数据库
  rpc SaveAdress(InspectionReq) returns (common.Response) {};
}

//用inspectionId查询车辆请求参数
message InspectionReq {
    uint64 userId = 1;
    string salesUrl = 2;
    string othersUrl = 3;
}